import RequestReset from '../../components/RequestReset/reset';
import Page from '../../components/Page/index';

const RequestResetPage = props => (
  <Page>
    <RequestReset />
  </Page>
);

export default RequestResetPage;
